<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta content="width=device-width,initial-scale=1.0" name="viewport"/>
<title>{{.Title}} (Printable)</title>
<style>
  :root {
    --primary-font: 'Georgia', serif;
    --spacing-unit: 1.5rem;
  }
  
  body { 
    font-family: var(--primary-font); 
    line-height: 1.6;
    max-width: 800px;
    margin: 0 auto;
    padding: 40px;
    color: #222;
  }

  /* Header Section for Student Details */
  .worksheet-header {
    display: flex;
    justify-content: space-between;
    margin-bottom: 40px;
    border-bottom: 2px solid #000;
    padding-bottom: 20px;
  }
  .student-field {
    border-bottom: 1px solid #999;
    width: 200px;
    margin-top: 5px;
  }

  h1 { text-align: center; text-transform: uppercase; letter-spacing: 1px; font-size: 24pt; margin-bottom: 10px; }
  p.instructions { text-align: center; font-style: italic; color: #555; margin-bottom: 40px; }

  /* Numbering and Layout */
  ol { padding-left: 0; list-style-position: inside; }
  
  .question { 
    margin-bottom: var(--spacing-unit); 
    page-break-inside: avoid; /* CRITICAL: Prevents questions from being cut in half when printing */
  }
  
  .question p { display: inline; font-weight: bold; }
  
  /* Multiple Choice Layout */
  .options-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* Creates neat columns */
    gap: 10px;
    margin-top: 10px;
    margin-left: 25px; /* Indent slightly */
    font-weight: normal;
  }

  @media print {
    body { font-size: 12pt; padding: 0; margin: 1cm; }
    button { display: none; } /* Hide any web-only buttons */
  }
</style>
</head>
<body>

<h1>{{.Title}}</h1>
<p class="instructions">{{.Instructions}}</p>

<ol>
{{range $i, $q := .Questions}}
  <li class="question">
    <p>{{$q.Text}}</p>

    {{if eq $q.Type 2}}
      <p>
      {{range $j, $opt := $q.Options}}
        {{printf "%c" (add $j 65)}}) {{$opt}}&nbsp;&nbsp;
      {{end}}
      </p>
    {{end}}
  </li>
{{end}}
</ol>

</body>
</html>
